{
  "name": "orbit-04",
  "productName": "ORBIT 04",
  "version": "0.63.1",
  "description": "Top-down space survival game built with JavaScript, Phaser 3, and Electron.",
  "main": "desktop/main.cjs",
  "private": true,
  "scripts": {
    "vendor": "node tools/vendor-phaser.mjs",
    "audio:build": "node tools/generate-audio-reforged.mjs",
    "postinstall": "npm run vendor",
    "start": "npm run vendor && electron .",
    "screenshot": "npm run vendor && electron . -- --orbit-capture",
    "package": "npm run vendor && electron-forge package",
    "make": "npm run vendor && electron-forge make",
    "test:assets": "node tests/assets.mjs",
    "test": "node --check desktop/main.cjs && node --check visual-engine.js && node --check tools/generate-audio-reforged.mjs && node --check game.js && node tests/smoke.js && node tests/top-down-polish.mjs && npm run test:assets"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@electron-forge/cli": "latest",
    "@electron-forge/maker-zip": "latest",
    "electron": "43.2.0"
  }
}
